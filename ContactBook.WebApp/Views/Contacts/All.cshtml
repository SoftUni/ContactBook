@model IEnumerable<ContactViewModel>

@{
    ViewData["Title"] = "Contact Book";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr/>

<div class="contacts-grid">
    @if (!Model.Any())
    {
        <h3 class="text-center">No contacts yet!</h3>
    }
    else
    {
        @await Html.PartialAsync("~/Views/Shared/_ContactPartial.cshtml", Model)
    }
</div>
